#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: Always returns 0 to indicate successful execution
 */
int main(void)
{
	int d;

	for (d = 0; d <= 9; d++)
		printf("%i", d);
	printf("\n");

	return (0);
}

#include <stdlib.h>
#include "main.h"
/**
 * _abs - Computes the absolute value of an integer
 * @n: The number to compute the absolute value to
 * Return: The absolute value
 */
int _abs(int n)
{
	return (abs(n));
}
